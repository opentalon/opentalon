You are improving an existing skill based on a new execution of a similar task.

Existing skill:
{{.ExistingSkill}}

New execution:
User request: {{.UserMessage}}
Tool calls executed:
{{.ToolTrace}}

Compare the existing skill with this new execution. If the new execution used fewer steps, recovered from errors better, or found a more effective approach, produce an improved version of the skill.

If the existing skill is already optimal, output it unchanged.

Output ONLY the YAML block, no explanation or markdown fences.