Given the following completed task session, extract a reusable skill that can guide future similar requests.

User request: {{.UserMessage}}

Tool calls executed (in order):
{{.ToolTrace}}

Extract a YAML skill definition with these fields:
- name: a short kebab-case identifier (e.g. "create-jira-from-slack")
- trigger: a generalized version of the user request (replace specific values with placeholders like <project>, <message>, etc.)
- steps: ordered list with plugin, action, and a brief description of each step's purpose
- preconditions: what must be true for this skill to work (e.g. required access, prior state)
- error_handling: recovery steps observed or recommended
- outcome: expected result description

Output ONLY the YAML block, no explanation or markdown fences.